<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/dragHandle"
    android:layout_width="wrap_content"
    android:layout_height="36dp"
    android:background="@drawable/overlay_bar_bg"
    android:elevation="6dp"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingStart="10dp"
    android:paddingEnd="6dp">

    <TextView
        android:id="@+id/overlayTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="淘金助手"
        android:textColor="#F5EDE4"
        android:textSize="11sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/btnOverlayExpand"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="4dp"
        android:background="@drawable/bg_overlay_btn"
        android:paddingHorizontal="8dp"
        android:paddingVertical="4dp"
        android:text="日志"
        android:textColor="#6EB5E8"
        android:textSize="11sp" />

    <TextView
        android:id="@+id/btnOverlayToggle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_overlay_btn"
        android:paddingHorizontal="8dp"
        android:paddingVertical="4dp"
        android:text="继续"
        android:textColor="#6BCB8B"
        android:textSize="11sp"
        android:textStyle="bold" />
</LinearLayout>
